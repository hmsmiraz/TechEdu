# Points directly at content-service for now. Once the gateway exists,
# point this at it instead and keep the /modules path the same.
NEXT_PUBLIC_CONTENT_API_URL=http://localhost:8001

# Where to send students to log in (the landing app)
NEXT_PUBLIC_LANDING_URL=http://localhost:3000
